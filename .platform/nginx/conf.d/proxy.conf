# Raise the reverse-proxy upload limit. Elastic Beanstalk's nginx defaults to a
# small client_max_body_size, so dealer document/photo uploads over ~1MB were
# rejected with 413 before reaching the app. Match the app's server-action limit
# (40MB in next.config.mjs) with headroom for multipart overhead.
client_max_body_size 50M;
